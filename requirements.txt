Flask==3.1.1
gunicorn==22.0.0              # Required by Render to run app
numpy                         # Needed by your ML model
opencv-python==4.12.0.88      # For video/image input
scikit-learn==1.7.1           # ML model
mediapipe==0.10.14            # Hand detection (if you're using it)
pyttsx3==2.99                 # Only if you're doing text-to-speech
joblib                        # For model loading/saving
